# Auth Service

Spring Boot Authentication Service with JWT, Redis, and Kafka.

## Features
- Login (JWT + Refresh Token)
- Password Reset (Email flow)
- Vendor Registration
- Admin User Management
- Audit Logging (Kafka)

## Running Locally
1. `docker-compose up -d`
2. `mvn spring-boot:run`

See `/docs` for more details.
